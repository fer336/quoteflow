{
  "private": true,
  "scripts": {
    "dev:tenant": "npm --prefix frontend run dev:tenant",
    "dev:cms": "npm --prefix frontend run dev:cms"
  },
  "dependencies": {
    "@react-oauth/google": "^0.13.4",
    "jwt-decode": "^4.0.0"
  }
}
