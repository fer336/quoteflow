# Frontend shared layer

Acá van utilidades, UI reutilizable y servicios cross-feature.

## Regla

- `shared/*` no debe depender de `features/*`.
- Si algo empieza a conocer una vertical específica, deja de ser shared.
